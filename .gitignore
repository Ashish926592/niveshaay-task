# Secrets — never commit
.env

# Node
node_modules/
npm-debug.log*

# n8n local data (if mounted locally)
.n8n/

# OS / editor
.DS_Store
*.log

# scratch (contains the revoked key for the interviewer)
KEY_PROOF.txt

# TypeScript build output
dist/

# ──────────────────────────────────────────────────────────────────────────
# Niveshaay Financial Results Pipeline — environment variables
# Copy this file to `.env` and fill in your values. NEVER commit `.env`.
# These are read by the n8n container (docker-compose `env_file`) and referenced
# inside the workflow as {{ $env.VAR_NAME }} — so no secret ever lives in workflow.json.
# ──────────────────────────────────────────────────────────────────────────

# REQUIRED — Google Gemini API key (https://aistudio.google.com/apikey)
GEMINI_API_KEY=your_gemini_api_key_here

# OPTIONAL — WhatsApp delivery (image) via Evolution API + the Puppeteer image-service.
# On a FRESH extraction the workflow renders the P&L PNG (IMAGE_SERVICE_URL /generate-image)
# and posts it to WHATSAPP_GROUP_JID via Evolution sendMedia. Leave WHATSAPP_GROUP_JID
# EMPTY to skip sending entirely (the UI/JSON still work).
EVOLUTION_API_URL=http://evolution:8080
EVOLUTION_API_KEY=change-me
EVOLUTION_INSTANCE=niveshaay
WHATSAPP_GROUP_JID=
IMAGE_SERVICE_URL=http://image-service:3001
# (Local-demo values when n8n is a container and Evolution/image-service run on the host:
#  EVOLUTION_API_URL=http://host.docker.internal:8080  IMAGE_SERVICE_URL=http://host.docker.internal:3001)

# REQUIRED: let nodes read env vars in expressions (the Gemini node uses
# {{ $env.GEMINI_API_KEY }}). n8n blocks this by default -> "access to env vars denied".
N8N_BLOCK_ENV_ACCESS_IN_NODE=false
